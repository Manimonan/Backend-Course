<!DOCTYPE html>
<html lang="en">
<head>
    <%- include('../partials/head') %>
    <title>Login to Airbnb</title> <% // Added a specific title %>
    <script src="https://cdn.tailwindcss.com"></script> <% // Ensure Tailwind CSS is linked %>
</head>
<body> 
    <%- include('../partials/nav') %> <% // Assuming nav is fixed or positioned appropriately %>

    <main class="container mx-auto p-8 max-w-md bg-white rounded-xl shadow-2xl relative"> <% // Larger shadow, more rounded corners, max-width adjusted %>
        <h1 class="text-4xl font-extrabold mb-4 text-center text-gray-800">Welcome Back!</h1> <% // More prominent and inviting heading %>
        <p class="text-center text-gray-600 mb-8">Log in to continue your Airbnb journey.</p> <% // Added a friendly sub-heading %>

        <form action="/login" method="POST" class="space-y-6"> <% // Increased vertical spacing between form elements %>
           
            <div> <% // Wrapped inputs in divs for better control over spacing and potential error messages %>
                <label for="email" class="block text-sm font-medium text-gray-700 mb-1">Email Address</label> <% // Added labels %>
                <input
                    type="email"
                    id="email"
                    name="email"
                    placeholder="you@example.com"
                    class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-red-500 focus:border-transparent transition duration-200"
                    required
                />
            </div>

            <div>
                <label for="password" class="block text-sm font-medium text-gray-700 mb-1">Password</label>
                <input
                    type="password"
                    id="password"
                    name="password"
                    placeholder="Enter your password"
                    class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-red-500 focus:border-transparent transition duration-200"
                    required
                />
                <a href="#" class="block text-right text-sm text-red-600 hover:underline mt-2">Forgot password?</a> <% // Added a "Forgot password" link %>
            </div>
            
            <button type="submit" class="w-full bg-red-600 text-white font-bold py-3 rounded-lg hover:bg-red-700 transition duration-300 transform hover:scale-105 focus:outline-none focus:ring-2 focus:ring-red-500 focus:ring-offset-2">
                Login
            </button>

            <p class="text-center text-sm text-gray-600 mt-4">
                Don't have an account yet? <a href="/signup" class="text-red-600 hover:underline font-medium">Sign up here</a>
            </p>
        </form>
    </main>
</body>
</html>