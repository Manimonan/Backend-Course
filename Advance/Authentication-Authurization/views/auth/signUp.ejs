<!DOCTYPE html>
<html lang="en">
<head>
    <%- include('../partials/head') %>
    <title>Register on Airbnb</title> <% // Added a more specific title %>
    <script src="https://cdn.tailwindcss.com"></script> <% // Ensure Tailwind CSS is linked %>
</head>
<body> <% // Centered content, light gray background %>
    <%- include('../partials/nav') %> <% // Assuming nav is fixed or positioned appropriately %>

    <main class="container mx-auto p-8 max-w-lg bg-white rounded-xl shadow-2xl relative "> <% // Larger shadow, more rounded corners, max-width adjusted %>
        <h1 class="text-4xl font-extrabold mb-8 text-center text-gray-800">Join Airbnb</h1> <% // More prominent heading %>
        <p class="text-center text-gray-600 mb-6">Create an account to list your home or book amazing stays.</p> <% // Added a friendly sub-heading %>

        <form action="/signup" method="POST" class="space-y-5"> <% // Increased vertical spacing between form elements %>

            <div> <% // Wrapped inputs in divs for better control over spacing and potential error messages %>
                <label for="name" class="block text-sm font-medium text-gray-700 mb-1">Full Name</label> <% // Added labels %>
                <input
                    type="text"
                    id="name"
                    name="name"
                    placeholder="e.g., John Doe"
                    class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-red-500 focus:border-transparent transition duration-200"
                    required
                />
            </div>

            <div>
                <label for="email" class="block text-sm font-medium text-gray-700 mb-1">Email Address</label>
                <input
                    type="email"
                    id="email"
                    name="email"
                    placeholder="e.g., you@example.com"
                    class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-red-500 focus:border-transparent transition duration-200"
                    required
                />
            </div>

            <div>
                <label for="password" class="block text-sm font-medium text-gray-700 mb-1">Password</label>
                <input
                    type="password"
                    id="password"
                    name="password"
                    placeholder="Create a strong password"
                    class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-red-500 focus:border-transparent transition duration-200"
                    required
                />
            </div>

            <div>
                <label for="confPassword" class="block text-sm font-medium text-gray-700 mb-1">Confirm Password</label>
                <input
                    type="password"
                    id="confPassword"
                    name="confPassword"
                    placeholder="Re-enter your password"
                    class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-red-500 focus:border-transparent transition duration-200"
                    required <% // Added required for confirm password %>
                />
            </div>

            <div class="border border-gray-300 rounded-lg py-3 px-4 bg-gray-50"> <% // Slightly different background for user type section %>
                <p class="text-base font-medium text-gray-800 mb-3">What kind of user are you?</p>
                <div class="flex flex-col sm:flex-row space-y-3 sm:space-y-0 sm:space-x-6"> <% // Stacked vertically on small screens, horizontal on larger %>
                    <label class="flex items-center cursor-pointer">
                        <input type="radio" name="userType" value="User" class="mr-3 h-4 w-4 text-red-600 focus:ring-red-500 border-gray-300 rounded" checked> <% // Default to "User", custom radio style %>
                        <span class="text-gray-800">Guest (Book Stays)</span>
                    </label>
                    <label class="flex items-center cursor-pointer">
                        <input type="radio" name="userType" value="host" class="mr-3 h-4 w-4 text-red-600 focus:ring-red-500 border-gray-300 rounded">
                        <span class="text-gray-800">Host (List Your Home)</span>
                    </label>
                </div>
            </div>

            <div class="flex items-start"> <% // Aligned checkbox text better %>
                <input type="checkbox" name="turmsAccept" value="turmsAccept" id="turmsAccept" class="mt-1 mr-3 h-4 w-4 text-red-600 focus:ring-red-500 border-gray-300 rounded" required> <% // Added required and custom checkbox style %>
                <label for="turmsAccept" class="text-sm text-gray-700 leading-tight">I agree to Airbnb's <a href="#" class="text-red-600 hover:underline">Terms of Service</a> and <a href="#" class="text-red-600 hover:underline">Privacy Policy</a>.</label> <% // Linked terms and conditions, improved text %>
            </div>

            <button type="submit" class="w-full bg-red-600 text-white font-bold py-3 rounded-lg hover:bg-red-700 transition duration-300 transform hover:scale-105 focus:outline-none focus:ring-2 focus:ring-red-500 focus:ring-offset-2">
                Create Account
            </button>
            <p class="text-center text-sm text-gray-600 mt-4">
                Already have an account? <a href="/login" class="text-red-600 hover:underline font-medium">Log in here</a>
            </p>
        </form>
    </main>
</body>
</html>